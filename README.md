# Mental Health Dashboard

แอปพลิเคชันสำหรับวิเคราะห์และแสดงผลข้อมูล Social Media Posts เกี่ยวกับสุขภาพจิตของเด็กและวัยรุ่น

## ข้อกำหนดเบื้องต้น

- Node.js (แนะนำเวอร์ชัน 16 ขึ้นไป)
- MySQL Server (XAMPP, WAMP, หรือ MySQL Workbench)
- Database: `mental_health_db` พร้อมตาราง `mental_health`

## การติดตั้งและรัน

### 1. เตรียม Database

เปิด MySQL Server (XAMPP/WAMP/MySQL Workbench) และตรวจสอบว่ามี:
- Database: `mental_health_db`
- Table: `mental_health` พร้อมคอลัมน์:
  - `id` (int) - Primary key
  - `username` (varchar) - ชื่อผู้ใช้
  - `caption` (text) - ข้อความโพสต์
  - `platform` (varchar) - แพลตฟอร์ม (facebook, instagram, twitter, tiktok)
  - `baseurl` (varchar) - ลิงก์โพสต์
  - `created_at` (timestamp) - วันที่สร้างโพสต์

### 2. ติดตั้ง Backend

```powershell
cd backend
npm install
```

รัน backend server:
```powershell
npm run dev
```

Backend จะรันที่ `http://localhost:5151`

### 3. ติดตั้ง Frontend

เปิด terminal ใหม่:
```powershell
cd frontend
npm install
```

รัน frontend:
```powershell
npm run dev
```

Frontend จะรันที่ `http://localhost:5173` (หรือ port ที่ Vite กำหนด)

### 4. เปิดใช้งาน

เปิด browser ไปที่ `http://localhost:5173`

## วิธีรันแบบง่าย (PowerShell)

ใช้ script สำเร็จรูป:
```powershell
.\start.ps1
```

Script จะเปิด 2 หน้าต่างให้อัตโนมัติ:
- หน้าต่างที่ 1: Backend Server
- หน้าต่างที่ 2: Frontend Dev Server

## API Endpoints

### GET /api/posts
ดึงข้อมูลโพสต์จาก database พร้อมตัวกรอง

**Query Parameters:**
- `startDate` - วันที่เริ่มต้น (format: YYYY-MM-DD)
- `endDate` - วันที่สิ้นสุด (format: YYYY-MM-DD)
- `platform` - แพลตฟอร์ม (facebook, instagram, twitter, tiktok)
- `searchTerm` - คำค้นหาในข้อความ

**ตัวอย่าง:**
```
GET http://localhost:5151/api/posts?startDate=2025-11-01&endDate=2025-12-09&platform=facebook
```

**Response:**
```json
[
  {
    "id": 1,
    "username": "user1",
    "caption": "โพสต์ตัวอย่าง",
    "platform": "facebook",
    "baseurl": "https://facebook.com/post1",
    "createAt": "2025-12-01T10:00:00.000Z"
  }
]
```

### GET /api/test
ทดสอบการเชื่อมต่อ backend และแสดง database config

**Response:**
```json
{
  "message": "Backend is working!",
  "dbConfig": {
    "host": "127.0.0.1",
    "user": "root",
    "database": "mental_health_db"
  }
}
```

## ฟีเจอร์หลัก

- 📊 Dashboard แสดงสถิติโพสต์รวม
- 📈 กราฟแนวโน้มการโพสต์ตามช่วงเวลา
- 🥧 กราฟแพลตฟอร์มยอดนิยม
- 📊 กราฟผู้ใช้งานที่โพสต์มากที่สุด 10 อันดับ
- 🔍 ตัวกรองตามวันที่และแพลตฟอร์ม
- 📋 ตารางแสดงรายการโพสต์พร้อม pagination
- 🔗 ลิงก์ไปยังโพสต์ต้นฉบับ

## โครงสร้างโปรเจค

```
dashboard/
├── backend/
│   ├── .env              # การตั้งค่า database
│   ├── server.js         # Express server + MySQL
│   ├── package.json
│   └── setup.sql         # SQL สำหรับสร้างตารางตัวอย่าง
├── frontend/
│   ├── src/
│   │   ├── App.jsx       # หน้าหลัก Dashboard
│   │   └── main.jsx
│   ├── package.json
│   └── vite.config.js
├── start.ps1             # Script รันทั้ง backend และ frontend
└── README.md
```

## การแก้ปัญหา

### Backend ไม่สามารถเชื่อมต่อ Database
- ตรวจสอบว่า MySQL Server เปิดอยู่
- ตรวจสอบค่าใน `.env` ว่าถูกต้อง
- ตรวจสอบว่ามี database `mental_health_db` และตาราง `mental_health`

### Frontend แสดง Error 500
- ตรวจสอบว่า Backend รันอยู่ที่ port 5151
- ตรวจสอบ Backend logs ใน terminal
- ลอง refresh หน้า browser

### Frontend ไม่แสดงข้อมูล
- ตรวจสอบว่ามีข้อมูลในตาราง `mental_health`
- ตรวจสอบ Console ใน browser (F12)
- ลองเปลี่ยนช่วงวันที่ในตัวกรอง

## เทคโนโลยีที่ใช้

**Backend:**
- Express.js - Web framework
- MySQL2 - Database driver
- dotenv - Environment variables
- CORS - Cross-origin resource sharing

**Frontend:**
- React - UI library
- Vite - Build tool
- Recharts - Chart library
- date-fns - Date formatting
- Lucide React - Icons
